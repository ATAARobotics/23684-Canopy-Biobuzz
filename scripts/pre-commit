#!/usr/bin/env bash
# biobuzz-hook-v2
# Auto-format staged Java files with dprint.
set -euo pipefail

if ! command -v dprint >/dev/null 2>&1; then
    exit 0
fi

dprint fmt TeamCode/

# Re-stage any files dprint touched that were already tracked.
git add -u